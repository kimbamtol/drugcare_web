const express = require('express');
const router = express.Router();
const userController = require('../controllers/userController'); // 경로 확인

// 사용자 관련 라우트 정의
// 예: router.post('/register', userController.register);

module.exports = router;
